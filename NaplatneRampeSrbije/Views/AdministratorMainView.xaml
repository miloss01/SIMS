<Window x:Class="NaplatneRampeSrbije.Views.AdministratorMainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NaplatneRampeSrbije.Views"
        mc:Ignorable="d"
        Title="AdministratorMainView" Height="450" Width="800">
    <Grid>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_CRUD">
                    <MenuItem Header="_Naplatno mesto CRUD"
                              x:Name="naplatnoMestoCRUD"
                              Click="naplatnoMestoCRUD_Click" />
                    <MenuItem Header="_Korisnik CRUD"
                              x:Name="korisnikCRUD" 
                              Click="korisnikCreate_Click" />
                </MenuItem>
            </Menu>
            <StackPanel></StackPanel>
        </DockPanel>
    </Grid>
</Window>
